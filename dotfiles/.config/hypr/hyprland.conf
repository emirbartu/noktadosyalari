# >>> START MERGE: monitor.conf
monitor = , preferred, auto, 1# <<< END MERGE: monitor.conf

# -----------------------------------------------------
# Cursor
# -----------------------------------------------------

# >>> START MERGE: cursor.conf
exec-once = hyprctl setcursor Bibata-Modern-Ice 24
# <<< END MERGE: cursor.conf


# -----------------------------------------------------
# Environment
# -----------------------------------------------------

# >>> START MERGE: environment.conf

# >>> START MERGE: default.conf
# -----------------------------------------------------
# Environment Variables
# name: "Default"
# -----------------------------------------------------

# Default Settings in ml4w.conf
# <<< END MERGE: default.conf

# <<< END MERGE: environment.conf


# -----------------------------------------------------
# Keyboard
# -----------------------------------------------------

# >>> START MERGE: keyboard.conf
# -----------------------------------------------------
# Keyboard Layout
# https://wiki.hyprland.org/Configuring/Variables/#input
# -----------------------------------------------------
# /home/username/.config/hypr/conf/keyboard.conf
input {
    kb_layout = tr, us
    kb_variant = 
    kb_model =
    kb_options = grp:win_space_toggle
    numlock_by_default = true
    mouse_refocus = false

    follow_mouse = 1
    touchpad {
        # for desktop
        natural_scroll = false

        # for laptop
        # natural_scroll = yes
        # middle_button_emulation = true
        # clickfinger_behavior = false
        scroll_factor = 1.0  # Touchpad scroll factor
    }
    sensitivity = 0 # Pointer speed: -1.0 - 1.0, 0 means no modification.
}

# -----------------------------------------------------
# Load color file
# -----------------------------------------------------

# >>> START MERGE: colors.conf

$background = rgba(1a110fff)

$error = rgba(ffb4abff)

$error_container = rgba(93000aff)

$inverse_on_surface = rgba(392e2bff)

$inverse_primary = rgba(8f4c35ff)

$inverse_surface = rgba(f1dfdaff)

$on_background = rgba(f1dfdaff)

$on_error = rgba(690005ff)

$on_error_container = rgba(ffdad6ff)

$on_primary = rgba(55200cff)

$on_primary_container = rgba(ffdbd0ff)

$on_primary_fixed = rgba(390c00ff)

$on_primary_fixed_variant = rgba(723520ff)

$on_secondary = rgba(442a21ff)

$on_secondary_container = rgba(ffdbd0ff)

$on_secondary_fixed = rgba(2c160eff)

$on_secondary_fixed_variant = rgba(5d4036ff)

$on_surface = rgba(f1dfdaff)

$on_surface_variant = rgba(d8c2bbff)

$on_tertiary = rgba(3a3005ff)

$on_tertiary_container = rgba(f3e2a7ff)

$on_tertiary_fixed = rgba(221b00ff)

$on_tertiary_fixed_variant = rgba(51461aff)

$outline = rgba(a08d87ff)

$outline_variant = rgba(53433fff)

$primary = rgba(ffb59dff)

$primary_container = rgba(723520ff)

$primary_fixed = rgba(ffdbd0ff)

$primary_fixed_dim = rgba(ffb59dff)

$scrim = rgba(000000ff)

$secondary = rgba(e7bdb0ff)

$secondary_container = rgba(5d4036ff)

$secondary_fixed = rgba(ffdbd0ff)

$secondary_fixed_dim = rgba(e7bdb0ff)

$shadow = rgba(000000ff)

$source_color = rgba(f96732ff)

$surface = rgba(1a110fff)

$surface_bright = rgba(423733ff)

$surface_container = rgba(271d1bff)

$surface_container_high = rgba(322825ff)

$surface_container_highest = rgba(3d322fff)

$surface_container_low = rgba(231917ff)

$surface_container_lowest = rgba(140c0aff)

$surface_dim = rgba(1a110fff)

$surface_tint = rgba(ffb59dff)

$surface_variant = rgba(53433fff)

$tertiary = rgba(d7c68dff)

$tertiary_container = rgba(51461aff)

$tertiary_fixed = rgba(f3e2a7ff)

$tertiary_fixed_dim = rgba(d7c68dff)
# <<< END MERGE: colors.conf

$color8 = $primary
$color11 = $on_surface

# -----------------------------------------------------
# Autostart
# -----------------------------------------------------

# >>> START MERGE: autostart.conf
#    ___       __           __           __
#   / _ |__ __/ /____  ___ / /____ _____/ /_
#  / __ / // / __/ _ \(_-</ __/ _ `/ __/ __/
# /_/ |_\_,_/\__/\___/___/\__/\_,_/_/  \__/
#

# Start Listeners
# exec-once=~/.config/ml4w/listeners.sh --startall

# Start Polkit
exec-once=/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Load Wallpaper
exec-once = ~/.config/hypr/scripts/wallpaper-restore.sh

# Load Notification Daemon
exec-once = swaync

# Load GTK settings
exec-once = ~/.config/hypr/scripts/gtk.sh

# Using hypridle to start hyprlock
exec-once = hypridle

# Load cliphist history
exec-once = wl-paste --watch cliphist store

# Start autostart cleanup
exec-once = ~/.config/hypr/scripts/cleanup.sh
# <<< END MERGE: autostart.conf


# -----------------------------------------------------
# Load configuration files
# -----------------------------------------------------

# >>> START MERGE: window.conf

# >>> START MERGE: default.conf
# -----------------------------------------------------
# General window layout and colors
# name: "Default"
# -----------------------------------------------------

general {
    gaps_in = 5
    gaps_out = 15
    border_size = 2
    col.active_border = $color11
    col.inactive_border = $color8
    layout = dwindle
    resize_on_border = true
}
# <<< END MERGE: default.conf

# <<< END MERGE: window.conf


# >>> START MERGE: decoration.conf

# >>> START MERGE: default.conf
decoration {
    rounding = 0
    active_opacity = 1.0
    inactive_opacity = 0.9
    fullscreen_opacity = 1.0

    shadow {
        enabled = true
        range = 10
        render_power = 2
        color = 0x33000000
    }
}
# <<< END MERGE: default.conf

# <<< END MERGE: decoration.conf


# >>> START MERGE: layout.conf

# >>> START MERGE: default.conf
# -----------------------------------------------------
# Layouts
# -----------------------------------------------------

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    # Commented out due to compatibility reasons
    # new_status = master
}

binds {
  workspace_back_and_forth = false
  allow_workspace_cycles = true
  pass_mouse_when_bound = false
}
# <<< END MERGE: default.conf

# <<< END MERGE: layout.conf


# >>> START MERGE: workspace.conf

# >>> START MERGE: default.conf
# -----------------------------------------------------
# General workspace rules
# name: "Default"
# -----------------------------------------------------

# Assigning workspace to a certain monitor. Below are just examples
# workspace = 1, monitor:eDP-1
# workspace = 2, monitor:eDP-1
# workspace = 3, monitor:eDP-1
# workspace = 4, monitor:eDP-1
# workspace = 5, monitor:DP-2
# workspace = 6, monitor:DP-2
# workspace = 7, monitor:DP-2
# workspace = 8, monitor:DP-2

# Example rules (from https://wiki.hyprland.org/Configuring/Workspace-Rules/)
# workspace = 3, rounding:false, decorate:false
# workspace = name:coding, rounding:false, decorate:false, gapsin:0, gapsout:0, border:false, decorate:false, monitor:DP-1
# workspace = 8,bordersize:8
# workspace = name:Hello, monitor:DP-1, default:true
# workspace = name:gaming, monitor:desc:Chimei Innolux Corporation 0x150C, default:true
# workspace = 5, on-created-empty:[float] firefox
# workspace = special:scratchpad, on-created-empty:foot
# <<< END MERGE: default.conf

# <<< END MERGE: workspace.conf


# >>> START MERGE: misc.conf
# -----------------------------------------------------
# Misc settings
# -----------------------------------------------------

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    initial_workspace_tracking = 1
}
# <<< END MERGE: misc.conf


# >>> START MERGE: keybinding.conf
# -----------------------------------------------------
# Key bindings
# name: "Default"
# -----------------------------------------------------

# SUPER KEY
$mainMod = SUPER
$HYPRSCRIPTS = ~/.config/hypr/scripts
# $SCRIPTS = ~/.config/ml4w/scripts
bind = $mainMod SHIFT, S, exec, ~/.local/bin//hyprshot -m region --clipboard only

# Applications
bind = $mainMod, RETURN, exec, kitty    # Open the terminal
bind = $mainMod, E, exec, dolphin      # Open the filemanager
# bind = $mainMod CTRL, E, exec, ~/.config/ml4w/settings/emojipicker.sh # Open the emoji picker
bind = $mainMod CTRL, C, exec, gnome-calculator  # Open the calculator
# Display
bind = $mainMod SHIFT, mouse_down, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') + 0.5}") # Increase display zoom
bind = $mainMod SHIFT, mouse_up, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') - 0.5}") # Decrease display zoom
bind = $mainMod SHIFT, Z, exec, hyprctl keyword cursor:zoom_factor 1 # Reset display zoom

# Windows
bind = $mainMod, Q, killactive                                                              # Kill active window
bind = $mainMod SHIFT, Q, exec, hyprctl activewindow | grep pid | tr -d 'pid:' | xargs kill # Quit active window and all open instances
bind = $mainMod, F, fullscreen, 0                                                           # Set active window to fullscreen
bind = $mainMod, M, fullscreen, 1                                                           # Maximize Window
bind = $mainMod, T, togglefloating                                                          # Toggle active windows into floating mode
bind = $mainMod SHIFT, T, workspaceopt, allfloat                                            # Toggle all windows into floating mode
bind = $mainMod, J, togglesplit                                                             # Toggle split
bind = $mainMod, left, movefocus, l                                                         # Move focus left
bind = $mainMod, right, movefocus, r                                                        # Move focus right
bind = $mainMod, up, movefocus, u                                                           # Move focus up
bind = $mainMod, down, movefocus, d                                                         # Move focus down
bindm = $mainMod, mouse:272, movewindow                                                     # Move window with the mouse
bindm = $mainMod, mouse:273, resizewindow                                                   # Resize window with the mouse
bind = $mainMod SHIFT, right, resizeactive, 100 0                                           # Increase window width with keyboard
bind = $mainMod SHIFT, left, resizeactive, -100 0                                           # Reduce window width with keyboard
bind = $mainMod SHIFT, down, resizeactive, 0 100                                            # Increase window height with keyboard
bind = $mainMod SHIFT, up, resizeactive, 0 -100                                             # Reduce window height with keyboard
bind = $mainMod, G, togglegroup                                                             # Toggle window group
bind = $mainMod, K, swapsplit                                                               # Swapsplit
bind = $mainMod ALT, left, swapwindow, l                                                    # Swap tiled window left
bind = $mainMod ALT, right, swapwindow, r                                                   # Swap tiled window right
bind = $mainMod ALT, up, swapwindow, u                                                      # Swap tiled window up
bind = $mainMod ALT, down, swapwindow, d                                                    # Swap tiled window down
binde = ALT,Tab,cyclenext                                                                   # Cycle between windows
binde = ALT,Tab,bringactivetotop                                                            # Bring active window to the top

# Actions
bind = $mainMod CTRL, R, exec, hyprctl reload                                             # Reload Hyprland configuration
bind = $mainMod SHIFT, A, exec, $HYPRSCRIPTS/toggle-animations.sh                         # Toggle animations
bind = $mainMod, PRINT, exec, $HYPRSCRIPTS/screenshot.sh                                  # Take a screenshot
bind = $mainMod ALT, F, exec, $HYPRSCRIPTS/screenshot.sh --instant                        # Take an instant full-screen screenshot
bind = $mainMod ALT, S, exec, $HYPRSCRIPTS/screenshot.sh --instant-area                   # Take an instant area screenshot
bind = $mainMod CTRL, Q, exec, wlogout                          # Start wlogout
bind = $mainMod SHIFT, W, exec, ~/.config/hypr/scripts/waypaper.sh --random                                         # Change the wallpaper
bind = $mainMod CTRL, W, exec, ~/.config/hypr/scripts/waypaper.sh                                                   # Open wallpaper selector
bind = $mainMod ALT, W, exec, $HYPRSCRIPTS/wallpaper-automation.sh                        # Start random wallpaper script
bind = $mainMod CTRL, RETURN, exec, ~/.config/hypr/scripts/launcher.sh                    # Open application launcher
bind = $mainMod CTRL, K, exec, $HYPRSCRIPTS/keybindings.sh                                # Show keybindings
bind = $mainMod SHIFT, B, exec, ~/.config/waybar/launch.sh                                # Reload waybar
bind = $mainMod CTRL, B, exec, ~/.config/waybar/toggle.sh                                 # Toggle waybar
bind = $mainMod SHIFT, R, exec, $HYPRSCRIPTS/loadconfig.sh                                # Reload hyprland config
bind = $mainMod, V, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy                         # Open clipboard manager
bind = $mainMod CTRL, T, exec, ~/.config/waybar/themeswitcher.sh                          # Open waybar theme switcher
# bind = $mainMod CTRL, S, exec, flatpak run com.ml4w.settings                              # Open ML4W Dotfiles Settings app
bind = $mainMod ALT, G, exec, $HYPRSCRIPTS/gamemode.sh                                    # Toggle game mode
bind = $mainMod CTRL, L, exec, ~/.config/hypr/scripts/power.sh lock                       # Launch Hyprshade
bind = $mainMod SHIFT, H, exec, $HYPRSCRIPTS/hyprshade.sh                                 # Start wlogout
bind = CTRL, Tab, exec, rofi -show window                                   # Open Select Window Menu

# Workspaces
bind = $mainMod, 1, workspace, 1  # Open workspace 1
bind = $mainMod, 2, workspace, 2  # Open workspace 2
bind = $mainMod, 3, workspace, 3  # Open workspace 3
bind = $mainMod, 4, workspace, 4  # Open workspace 4
bind = $mainMod, 5, workspace, 5  # Open workspace 5
bind = $mainMod, 6, workspace, 6  # Open workspace 6
bind = $mainMod, 7, workspace, 7  # Open workspace 7
bind = $mainMod, 8, workspace, 8  # Open workspace 8
bind = $mainMod, 9, workspace, 9  # Open workspace 9
bind = $mainMod, 0, workspace, 10 # Open workspace 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1  # Move active window to workspace 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2  # Move active window to workspace 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3  # Move active window to workspace 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4  # Move active window to workspace 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5  # Move active window to workspace 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6  # Move active window to workspace 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7  # Move active window to workspace 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8  # Move active window to workspace 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9  # Move active window to workspace 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 # Move active window to workspace 10

bind = $mainMod, Tab, workspace, m+1       # Open next workspace
bind = $mainMod SHIFT, Tab, workspace, m-1 # Open previous workspace

bind = $mainMod CTRL, 1, exec, $HYPRSCRIPTS/moveTo.sh 1  # Move all windows to workspace 1
bind = $mainMod CTRL, 2, exec, $HYPRSCRIPTS/moveTo.sh 2  # Move all windows to workspace 2
bind = $mainMod CTRL, 3, exec, $HYPRSCRIPTS/moveTo.sh 3  # Move all windows to workspace 3
bind = $mainMod CTRL, 4, exec, $HYPRSCRIPTS/moveTo.sh 4  # Move all windows to workspace 4
bind = $mainMod CTRL, 5, exec, $HYPRSCRIPTS/moveTo.sh 5  # Move all windows to workspace 5
bind = $mainMod CTRL, 6, exec, $HYPRSCRIPTS/moveTo.sh 6  # Move all windows to workspace 6
bind = $mainMod CTRL, 7, exec, $HYPRSCRIPTS/moveTo.sh 7  # Move all windows to workspace 7
bind = $mainMod CTRL, 8, exec, $HYPRSCRIPTS/moveTo.sh 8  # Move all windows to workspace 8
bind = $mainMod CTRL, 9, exec, $HYPRSCRIPTS/moveTo.sh 9  # Move all windows to workspace 9
bind = $mainMod CTRL, 0, exec, $HYPRSCRIPTS/moveTo.sh 10 # Move all windows to workspace 10

bind = $mainMod, mouse_down, workspace, e+1  # Open next workspace
bind = $mainMod, mouse_up, workspace, e-1    # Open previous workspace
bind = $mainMod CTRL, down, workspace, empty # Open the next empty workspace

# Fn keys
bind = , XF86MonBrightnessUp, exec, brightnessctl -q s +10%                                                                                                  # Increase brightness by 10%
bind = , XF86MonBrightnessDown, exec, brightnessctl -q s 10%-                                                                                                # Reduce brightness by 10%
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+                                                                          # Increase volume by 5% (max 100% limit also added hold to raise volume)
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-                                                                               # Reduce volume by 5% (min 0% limit also added hold to lower volume)
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle                                                                                      # Toggle mute
bind = , XF86AudioPlay, exec, playerctl play-pause                                                                                                           # Audio play pause
bind = , XF86AudioPause, exec, playerctl pause                                                                                                               # Audio pause
bind = , XF86AudioNext, exec, playerctl next                                                                                                                 # Audio next
bind = , XF86AudioPrev, exec, playerctl previous                                                                                                             # Audio previous
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle                                                                               # Toggle microphone
bind = , XF86Calculator, exec, gnome-calculator                                                                                         # Open calculator
bind = , XF86Lock, exec, hyprlock                                                                                                                            # Open screenlock
# bind = , XF86Tools, exec, flatpak run com.ml4w.settings                                                                                                      # Open ML4W Dotfiles Settings app

bind = , code:238, exec, brightnessctl -d smc::kbd_backlight s +10
bind = , code:237, exec, brightnessctl -d smc::kbd_backlight s 10-

# <<< END MERGE: keybinding.conf


# >>> START MERGE: windowrule.conf

# >>> START MERGE: default.conf
# -----------------------------------------------------
# Window rules
# -----------------------------------------------------

windowrulev2 = float=1, title:^(pavucontrol)$
windowrulev2 = float=1, title:^(blueman-manager)$
windowrulev2 = float=1, title:^(nm-connection-editor)$
windowrulev2 = float=1, title:^(qalculate-gtk)$

# Browser Picture in Picture
windowrulev2 = float=1, title:^(Picture-in-Picture)$
windowrulev2 = pin=1, title:^(Picture-in-Picture)$
windowrulev2 = move=69.5% 4%, title:^(Picture-in-Picture)$

# idle inhibit
windowrulev2 = idleinhibit=fullscreen, class:([window])

# xwayland related rules
windowrulev2 = noblur=1, class:^(\bresolve\b)$, xwayland:1
# <<< END MERGE: default.conf

# <<< END MERGE: windowrule.conf


# -----------------------------------------------------
# Animation
# -----------------------------------------------------

# >>> START MERGE: animation.conf

# >>> START MERGE: default.conf

# name "End-4"
# credit https://github.com/end-4/dots-hyprland

animations {
    enabled = true
    # Animation curves
    
    bezier = linear, 0, 0, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92 
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.38, 0.04, 1, 0.07
    bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    bezier = softAcDecel, 0.26, 0.26, 0.15, 1
    bezier = md2, 0.4, 0, 0.2, 1 # use with .2s duration
    # Animation configs
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = windowsIn, 1, 3, md3_decel, popin 60%
    animation = windowsOut, 1, 3, md3_accel, popin 60%
    animation = border, 1, 10, default
    animation = fade, 1, 3, md3_decel
    # animation = layers, 1, 2, md3_decel, slide
    animation = layersIn, 1, 3, menu_decel, slide
    animation = layersOut, 1, 1.6, menu_accel
    animation = fadeLayersIn, 1, 2, menu_decel
    animation = fadeLayersOut, 1, 4.5, menu_accel
    animation = workspaces, 1, 7, menu_decel, slide
    # animation = workspaces, 1, 2.5, softAcDecel, slide
    # animation = workspaces, 1, 7, menu_decel, slidefade 15%
    # animation = specialWorkspace, 1, 3, md3_decel, slidefadevert 15%
    animation = specialWorkspace, 1, 3, md3_decel, slidevert
}
# <<< END MERGE: default.conf

# <<< END MERGE: animation.conf


# -----------------------------------------------------
# Environment for xdg-desktop-portal-hyprland
# -----------------------------------------------------
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# -----------------------------------------------------
# ML4W Configuration
# -----------------------------------------------------

# >>> START MERGE: ml4w.conf
# -----------------------------------------------------
# Floating windows
# -----------------------------------------------------

# Pavucontrol
windowrulev2 = float=1, class:(.*org.pulseaudio.pavucontrol.*)
windowrulev2 = size=700 600, class:(.*org.pulseaudio.pavucontrol.*)
windowrulev2 = center=1, class:(.*org.pulseaudio.pavucontrol.*)
windowrulev2 = pin=1, class:(.*org.pulseaudio.pavucontrol.*)

# OpenAI ChatGPT
windowrulev2 = float=1, title:(ChatGPT.*)
windowrulev2 = float=1, title:(.*chat.openai.com.*)
windowrulev2 = size=500 50%, title:(.*chat.openai.com.*)
windowrulev2 = move=20 70, title:(.*chat.openai.com.*)

# ML4W Configuration Removed

# Blueman
windowrulev2 = float=1, class:(blueman-manager)
windowrulev2 = size=800 600, class:(blueman-manager)
windowrulev2 = center=1, class:(blueman-manager)

# nwg-look
windowrulev2 = float=1, class:(nwg-look)
windowrulev2 = size=700 600, class:(nwg-look)
windowrulev2 = move=10% 20%, class:(nwg-look)
windowrulev2 = pin=1, class:(nwg-look)

# nwg-displays
windowrulev2 = float=1, class:(nwg-displays)
windowrulev2 = size=900 600, class:(nwg-displays)
windowrulev2 = move=10% 20%, class:(nwg-displays)
windowrulev2 = pin=1, class:(nwg-displays)

# Mission Center
windowrulev2 = float=1, class:(io.missioncenter.MissionCenter)
windowrulev2 = size=900 600, class:(io.missioncenter.MissionCenter)
windowrulev2 = center=1, class:(io.missioncenter.MissionCenter)
windowrulev2 = pin=1, class:(io.missioncenter.MissionCenter)

# Mission Center Preferences
windowrulev2 = float=1, class:(missioncenter), title:^(Preferences)$
windowrulev2 = center=1, class:(missioncenter), title:^(Preferences)$
windowrulev2 = pin=1, class:(missioncenter), title:^(Preferences)$

# Gnome Calculator
windowrulev2 = float=1, class:(org.gnome.Calculator)
windowrulev2 = size=700 600, class:(org.gnome.Calculator)
windowrulev2 = center=1, class:(org.gnome.Calculator)

# Hyprland Share Picker
windowrulev2 = float=1, class:(hyprland-share-picker)
windowrulev2 = size=600 400, class:(hyprland-share-picker)
windowrulev2 = center=1, class:(hyprland-share-picker)
windowrulev2 = pin=1, class:(hyprland-share-picker)

# Generic floating
windowrulev2 = float=1, class:(dotfiles-floating)
windowrulev2 = size=1000 700, class:(dotfiles-floating)
windowrulev2 = center=1, class:(dotfiles-floating)

# File pickers
windowrulev2 = float=1, class:(xdg-desktop-portal-gtk), title:^(Open.*Files?|Save.*Files?|All Files|Save)
windowrulev2 = center=1, class:(xdg-desktop-portal-gtk), title:^(Open.*Files?|Save.*Files?|All Files|Save)

# -----------------------------------------------------
# Layer rules (SwayNC)
# -----------------------------------------------------

layerrulev2 = blur=1, layer:swaync-control-center
layerrulev2 = blur=1, layer:swaync-notification-window
layerrulev2 = ignorezero=1, layer:swaync-control-center
layerrulev2 = ignorezero=1, layer:swaync-notification-window
layerrulev2 = ignorealpha=0.5, layer:swaync-control-center
layerrulev2 = ignorealpha=0.5, layer:swaync-notification-window

# -----------------------------------------------------
# Environment
# -----------------------------------------------------

env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1

env = GDK_SCALE,1
env = GDK_BACKEND,wayland,x11,*
env = CLUTTER_BACKEND,wayland

env = MOZ_ENABLE_WAYLAND,1
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = OZONE_PLATFORM,wayland
env = ELECTRON_OZONE_PLATFORM_HINT,wayland

xwayland {
    force_zero_scaling = true
}
# <<< END MERGE: ml4w.conf


# -----------------------------------------------------
# Custom
# -----------------------------------------------------

# >>> START MERGE: custom.conf
# Add your additional Hyprland configurations here
env = SDL_VIDEODRIVER,wayland

exec-once = vicinae server

# toggle vicinae
bind = ALT, Space, exec, vicinae toggle

# -----------------------------------------------------
# Vicinae layer rules
# -----------------------------------------------------
layerrule {
    name = vicinae-blur
    blur = on
    ignore_alpha = 0
    match:namespace = vicinae
}

# disable animation for vicinae only
layerrule {
    name = vicinae-no-animation
    no_anim = on
    match:namespace = vicinae
}
# <<< END MERGE: custom.conf

# <<< END MERGE: hyprland.conf

